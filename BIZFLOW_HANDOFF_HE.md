# BizFlow — מסמך מסירה והמשך פיתוח

תאריך מסירה: 24 ביולי 2026  
סביבת יעד: Expo SDK 54, React Native ו־iOS  
שם החבילה: `com.valeriya.bizflow`  
פרויקט EAS קיים: `5dbf67d4-a3db-4bcc-8de2-3ef3aabff009`  
בעלים ב־Expo: `lera2004`

## 1. מהו הפרויקט

BizFlow היא אפליקציה אישית לניהול פעילות פיננסית ועסקית בעברית ובכיוון RTL. היא כוללת:

- סקירה חודשית של הכנסות, תשלומים ממתינים, ניכויים וחלוקת כסף.
- תיקי לקוחות, פרויקטים, חשבוניות, קבלות, הצעות מחיר, NDA והסכמי עבודה.
- הוצאות עסקיות ואישיות, מנויים והוצאות חודשיות קבועות.
- חיסכון אישי, קופת עסק וקופת בזבוזים.
- תנועות בקופות, יעדי חיסכון והעברות מקופת הבזבוזים.
- אזור רואה חשבון, קבצים ודוח ZIP שנתי.
- אזור השראה לתוכן ברשתות חברתיות.
- צ׳אט מקומי בשם ג׳יני.

העיקרון החשוב ביותר: העיצוב המאושר נמצא בקובץ HTML אחד, והוא מוצג בתוך אפליקציית Expo ללא מסגרת טלפון מדומה.

## 2. מקור האמת הפעיל

הקבצים הפעילים בזמן הריצה הם:

```text
App.js
assets/bizflow/BizFlow.html
assets/bizflow/support.txt
```

- `App.js` הוא מעטפת React Native שמכינה את קובצי ה־HTML ומציגה אותם ב־WebView.
- `assets/bizflow/BizFlow.html` מכיל את העיצוב, המסכים, המודלים והלוגיקה העסקית הפעילה.
- `assets/bizflow/support.txt` הוא קובץ runtime שנארז כ־asset ומועתק בזמן ההפעלה בשם `support.js`.
- התיקייה `src/` מכילה ניסיון קודם למימוש React Native מלא. היא נשמרה לצורך היסטוריה, אך אינה מחוברת כרגע ל־`App.js` ואינה מקור האמת של הממשק.

אין לבצע עיצוב מחדש דרך הקבצים שבתיקיית `src/` בלי החלטה מפורשת על מעבר ארכיטקטורה.

## 3. ארכיטקטורת הריצה

בכל פתיחה של האפליקציה:

1. Expo טוען את `App.js`.
2. `App.js` מעתיק את `BizFlow.html`, את `support.txt` ואת שלוש הספריות שבתיקיית
   `assets/bizflow/vendor/` אל:

   ```text
   FileSystem.documentDirectory/bizflow-original/
   ```

3. `support.txt` נשמר בשם `support.js`, כדי שקובץ ה־HTML יוכל לטעון אותו מקומית.
   באותו אופן נשמרים `react.txt`, `react-dom.txt` ו־`babel.txt` בשמות
   `react.js`, `react-dom.js` ו־`babel.js`.
4. WebView פותח את `BizFlow.html` מהנתיב המקומי הקבוע.
5. בדיקת רוחב מוודאת שהאפליקציה ממלאת את רוחב המסך ולא מוצגת בתוך מסגרת או עמודה צרה.
   הבדיקה חוזרת על עצמה כל 150ms עד 20 שניות, כדי לא להיכשל על מכשיר איטי.
6. עד שה־WebView מוכן, מוצג מסך טעינה עדין עם סימן ₪ ואנימציית פעימה. אם הבדיקה
   נכשלת עד תום הזמן, מוצגת שורת הסבר במקום טעינה אינסופית.

הנתיב המקומי נשאר קבוע כדי שה־origin של WebView לא ישתנה, וכך `localStorage` ו־IndexedDB ממשיכים לזהות את אותו מאגר נתונים.

### ספריות מקומיות במקום CDN

`support.js` טוען React, ReactDOM ו־Babel. במקור הם נמשכו בזמן ריצה מ־`unpkg.com`,
ולכן ללא אינטרנט זמין `bizflow-app-root` נשאר ריק, בדיקת הרוחב נכשלה, והאפליקציה
נתקעה על מסך הטעינה ללא הודעת שגיאה.

שלושת הקבצים ארוזים כעת בתוך האפליקציה. `BizFlow.html` מגדיר `window.__resources`
לפני טעינת `support.js`, וממפה כל כתובת CDN לעותק המקומי. זהו מנגנון קיים של
ה־runtime ולא עקיפה שלו. הקבצים זהים בייט־לבייט למקור, ואומתו מול חתימות ה־SRI
שמופיעות ב־`support.txt`.

תלויות רשת שנותרו, וכולן אינן חוסמות טעינה:

```text
fonts.googleapis.com   הפונט Assistant; ללא רשת יוצג פונט חלופי
api.frankfurter.app    שער דולר; נשמר ב-localStorage מהפעם הקודמת
```

## 4. שמירת נתונים

הנתונים נשמרים במכשיר עצמו ולא בענן.

### localStorage

```text
bizflow_v3_data
bizflow_v3_saved_at
bizflow_fab_pos
bizflow_usd_rate
```

### IndexedDB

```text
Database: bizflow_storage
Object store: snapshots
Key: current
```

כל שינוי שמבוצע דרך פונקציית `save()`:

- מעדכן את ה־state.
- כותב את כל הנתונים ל־`localStorage`.
- שומר snapshot נוסף ב־IndexedDB.
- משתמש בחותמת זמן כדי לשחזר את העותק החדש ביותר.

נעשית גם בקשה ל־`navigator.storage.persist()` כאשר היא זמינה.

### מגבלה חשובה

הנתונים נשמרים מקומית במכשיר שבו האפליקציה רצה. מחיקה של האפליקציה, ניקוי נתוני Expo Go או מעבר למכשיר אחר עלולים למחוק או לא להעביר את המידע. אין כרגע חשבון משתמש, בסיס נתונים בענן או סנכרון בין מכשירים.

## 5. מבנה הנתונים המרכזי

האובייקט הראשי מכיל את המערכים הבאים:

```js
{
  clients: [],
  invoices: [],
  quotes: [],
  expenses: [],
  cash: [],
  shopping: [],
  fixed: [],
  costs: [],
  inspiration: []
}
```

### clients

לקוחות, סוג לקוח, שיטת תשלום, תאריך פתיחה וחוזה קבוע.

### invoices

חשבוניות/קבלות, פרויקט, סכום, סטטוס תשלום, תאריכים וקבצים מצורפים.

### quotes

הצעות מחיר, תוקף, אישור, מקדמה וקבלה על המקדמה.

### expenses

הוצאות אישיות או עסקיות, קטגוריה, סכום, תאריך וקבלה.

### cash

תנועות בקופות:

- `deposit` — הפקדה.
- `withdrawal` — משיכה.
- `isBusiness: true` — קופת העסק.
- `isBusiness: false` — חיסכון אישי.
- `source: "spending"` — העברה מקופת הבזבוזים.
- `source: "expense"` — משיכה שנוצרה אוטומטית מהוצאה עסקית.

### fixed

מנויים והוצאות חודשיות קבועות, לרבות מטבע ILS/USD וסיווג אישי/עסקי.

### costs

עלויות ישירות של פרויקט.

### shopping

יעדי חיסכון וקנייה.

### inspiration

רעיונות לתוכן, קישור, תיאור, דירוג כוכבים וסטטוס ביצוע.

## 6. לוגיקת הסקירה הראשית

### הכנסה חודשית

הכנסה מחושבת מחשבוניות שמועד התשלום שלהן בחודש הנבחר ומהצעות מחיר שאושרו.

### התקבל

הסכום שהתקבל כולל חשבוניות שסומנו כשולמו ומקדמות שסומנו כמשולמות.

### נשאר לחלוקה

```text
הכנסה - הוצאות חודשיות קבועות - עלויות פרויקט
```

היתרה יכולה להיות שלילית. לדוגמה, אם אין הכנסה וההוצאות הקבועות הן ₪715, הסקירה מציגה `-₪715`.

כאשר היתרה שלילית, הסכומים לחלוקה בין הקופות נשארים ₪0 ולא נוצרת חלוקה שלילית.

### חלוקה חיובית

- 50% חיסכון אישי.
- 25% קופת עסק.
- 25% בזבוזים.

## 7. קופת בזבוזים והעברות

קופת הבזבוזים מוצגת בסקירה לצד קופת העסק והחיסכון האישי.

מתחת לשלוש הקופות מוצג “סה״כ בעו״ש”, המחושב בזמן אמת כך:

```text
חיסכון אישי + קופת עסק + קופת בזבוזים
```

העברה פנימית מקופת הבזבוזים לאחת מקופות החיסכון אינה משנה את סך העו״ש: היא מפחיתה קופה אחת ומגדילה את האחרת באותו סכום.

היתרה הזמינה לבזבוזים:

- צוברת 25% מהיתרה החיובית בכל חודש.
- מפחיתה הוצאות אישיות.
- מפחיתה סכומים שהועברו לחיסכון אישי או לקופת העסק.
- לעולם אינה מוצגת מתחת לאפס במימוש הנוכחי.

באזור ההוצאות האישיות קיים כפתור העברה:

- אפשר להזין סכום ידני.
- אפשר לבחור “הכול”.
- אפשר לבחור חיסכון אישי או קופת עסק.
- ההעברה נשמרת כתנועת הפקדה בקופה שנבחרה.
- מחיקת ההעברה מחזירה את הסכום לקופת הבזבוזים.
- ניתן לערוך את סכום ההעברה או לשנות את קופת היעד.

## 8. סנכרון הוצאות וקופות

- הוצאה עסקית יוצרת אוטומטית משיכה מקופת העסק.
- הוצאה אישית מפחיתה אוטומטית את קופת הבזבוזים.
- עריכת הוצאה עסקית מעדכנת את המשיכה המקושרת.
- מחיקת הוצאה עסקית מוחקת גם את המשיכה המקושרת.
- מחיקת המשיכה המקושרת מתוך רשימת התנועות מוחקת גם את ההוצאה, כדי למנוע חוסר התאמה.
- משיכת קופה שנוצרה מהוצאה עסקית אינה נספרת פעם נוספת באזור רואה החשבון.
- אפשר לערוך את השם והסכום של הפקדות ומשיכות ידניות.

## 9. רואה חשבון

האזור מחלק את המידע לשלוש קבוצות:

1. קבלות ללקוחות.
2. חשבוניות והצעות מחיר.
3. הוצאות מוכרות.

הספירה “קבלות חסרות” מחושבת רק לפי קבלות שצריך להוציא בעקבות תשלומי לקוחות. חשבוניות, הצעות מחיר והוצאות עסקיות אינן מגדילות את המונה הזה.

הורדת ZIP מלא נחסמת כאשר חסרה קבלה נדרשת ללקוח.

ה־ZIP כולל:

- תיקיית קבלות ללקוחות.
- תיקיית קבלות/קבצים של הוצאות עסקיות.
- קובץ סיכום שנתי קצר.

הוצאות עסקיות ללא קובץ עדיין מופיעות באזור רואה החשבון וניתן לצרף אליהן קובץ.

## 10. לקוחות ופרויקטים

המערכת כוללת:

- סוגי לקוחות: חברה, בעל עסק, תלמיד וייעוץ.
- שיטות תשלום: שוטף 30, שוטף 60, 50/50, מלא מראש ומותאם.
- חישוב תאריך תשלום על בסיס שיטת התשלום.
- חשבונית, קבלה, הצעת מחיר, NDA והסכם עבודה.
- הסכם קבוע ברמת לקוח.
- תמיכה מיוחדת בלקוח Samsung: חוזה קבוע ברמת הלקוח ו־NDA ברמת פרויקט.
- הצעת מחיר אינה נספרת כהכנסה עד שהיא מאושרת.
- מקדמה ששולמה מתווספת לסכום שהתקבל.
- הצעת מחיר שפג תוקפה מוצגת כלא רלוונטית.
- רווח נקי ללקוח לפי חודש או שנה.

## 11. ג׳יני

ג׳יני היא כרגע לוגיקה מקומית בתוך קובץ ה־HTML ולא שירות AI בענן.

- אפשר לשאול שאלות בסיסיות על הכנסות, תשלומים, הוצאות וחסכונות.
- אפשר לבחור תמונות של הוצאות.
- המיון בין הוצאה עסקית לאישית מבוסס כרגע על כללי מילות מפתח.
- ציוד מחשוב, ציוד משרדי, תוכנות, מצלמות ומיקרופונים נוטים להיות מסווגים כעסקיים.

אין כרגע OCR אמיתי, מודל שפה מרוחק או אימות בנקאי.

## 12. השראה

באזור ההשראה אפשר:

- להוסיף קישור לסרטון.
- לתאר את הרעיון.
- לתת דירוג של 1–5 כוכבים.
- לסמן בוצע/לביצוע.
- לסנן לפי סטטוס.
- לפתוח את הקישור.
- לערוך או למחוק רעיון.

## 13. הרצה מקומית

דרישות מומלצות:

- Node.js מודרני. הפיתוח האחרון נבדק עם Node `v24.14.0`.
- pnpm. הפיתוח האחרון נבדק עם pnpm `11.9.0`.
- Expo Go במכשיר, או חשבון Expo לצורך EAS.

התקנה:

```bash
pnpm install
```

שרת פיתוח מקומי:

```bash
pnpm start
```

גישה ממכשיר דרך tunnel:

```bash
pnpm exec expo start --tunnel
```

בדיקת בנייה ל־iOS:

```bash
pnpm exec expo export --platform ios
```

Build פנימי:

```bash
pnpm exec eas build --profile preview --platform ios
```

Build ייצור:

```bash
pnpm exec eas build --profile production --platform ios
```

עדכון לענף preview:

```bash
pnpm run publish:preview
```

פעולות EAS דורשות התחברות לחשבון Expo שיש לו הרשאה לפרויקט של `lera2004`.

## 14. שרת Expo וקוד QR

קוד QR של `expo start --tunnel` הוא קישור לשרת פיתוח שרץ במחשב. הוא אינו פתרון קבוע:

- אם המחשב ישן או כבוי, השרת אינו זמין.
- אם התהליך נסגר, ה־tunnel מפסיק.
- אין להסתמך עליו כאפליקציית ייצור.

כדי שהאפליקציה תהיה זמינה באופן קבוע יש להשתמש ב־EAS Build להפצת אפליקציה מותקנת, וב־EAS Update לעדכוני קוד תואמים. נתונים משותפים בין מכשירים יחייבו גם backend או שירות ענן.

קובצי QR זמניים אינם כלולים בחבילת המסירה כדי שלא יטעו מפתח אחר לחשוב שמדובר בקישור קבוע.

## 15. בדיקות שבוצעו

הגרסה האחרונה נבדקה באמצעות:

- פענוח JavaScript של קוד ה־DC מתוך `BizFlow.html`.
- בדיקת התאמה בין תגי `sc-if` ו־`sc-for`.
- `git diff --check`.
- Expo export ל־iOS.
- טעינת `BizFlow.html` בדפדפן במצב offline מלא, והרצת אותה בדיקת רוחב
  שמופיעה ב־`App.js`.

תוצאת הבנייה האחרונה:

```text
iOS Bundled
593 modules
BizFlow.html, support.txt and vendor/* packaged successfully
```

תוצאת בדיקת ה־offline:

```text
לפני התיקון:  root=0    pageReady=false   (נכשל בטעינת react מ-unpkg)
אחרי התיקון:  root=390  pageReady=true    ללא שגיאות JavaScript
```

Baseline של תכונות הפיננסים והסנכרון:

```text
9475f4b274a07634990a97a50110835ee18c89c1
```

## 16. נקודות פתוחות שחייבים להכריע לפני המשך

### יתרת עו״ש ויתרות פתיחה

נמסרו שני נתונים שאינם תואמים:

```text
יתרת עו״ש שנאמרה: ₪9,690

חלוקה שנאמרה:
חיסכון אישי: ₪7,960
קופת עסק: ₪1,000
בזבוזים: ₪1,000
סה״כ החלוקה: ₪9,960
```

לכן לא הוזנו עדיין יתרות פתיחה ולא נבנה עדיין חישוב העו״ש החל מאוגוסט. אסור למפתח הבא לנחש את המספר.

יש לקבל תשובה לאחת משתי האפשרויות:

- עו״ש ₪9,690, ואז אם שתי הקופות האחרות נשארות ₪1,000 כל אחת — החיסכון האישי צריך להיות ₪7,690.
- עו״ש ₪9,960, ואז החיסכון האישי נשאר ₪7,960.

לאחר האישור מומלץ לשמור:

```js
bankOpeningBalance
bankOpeningDate // 2026-08-01
openingPersonalSavings
openingBusinessSavings
openingSpending
```

החישוב העתידי צריך להתחיל ב־1 באוגוסט 2026 ולכלול רק תנועות ממועד זה:

- הכנסות שהתקבלו בפועל.
- הוצאות אישיות ועסקיות.
- הוצאות קבועות חודשיות.
- עלויות פרויקט.
- משיכות עסקיות שאינן כפילות של הוצאה שכבר נספרה.

העברות פנימיות בין בזבוזים, חיסכון אישי וקופת עסק אינן משנות את סך העו״ש.

### ענן וגיבוי

עדיין אין:

- התחברות משתמש.
- מסד נתונים בענן.
- סנכרון בין מכשירים.
- גיבוי חיצוני אוטומטי.

### ייבוא קבלות ממייל

הממשק קיים, אך החיבור ל־Gmail/Outlook אינו ממומש. נדרש OAuth ושירות backend או API מאובטח.

### AI/OCR

מיון התמונות בג׳יני הוא rule-based. כדי לקבל זיהוי אמיתי יש להוסיף OCR ושירות AI, עם מנגנון אישור לפני שמירת הוצאה.

## 17. כללי המשך עבודה

1. לא לשנות צבעים, רקעים, ג׳יני, אנימציות או סגנון בלי בקשה מפורשת.
2. לבצע שינויים ב־`assets/bizflow/BizFlow.html`, שהוא מקור האמת הפעיל.
3. לא להחליף את האפליקציה במסכי `src/` בלי תהליך מעבר והצלבה מלא.
4. כל שינוי פיננסי חייב לעדכן את כל המקומות הרלוונטיים: סקירה, קופות, הוצאות ורואה חשבון.
5. כל פריט שהמשתמשת מוסיפה צריך לקבל אפשרות עריכה ומחיקה.
6. לא לאפס או להחליף נתונים קיימים בלי אישור מפורש.
7. להריץ export ל־iOS לפני מסירה.
8. לא ליצור QR חדש לכל שינוי; בזמן פיתוח מעדכנים את אותו פרויקט Expo. להפצה קבועה משתמשים ב־EAS Build/Update.

## 18. מה כלול בחבילת ה־ZIP

```text
App.js
app.json
eas.json
index.js
metro.config.js
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
.gitignore
assets/bizflow/BizFlow.html
assets/bizflow/support.txt
src/*
BIZFLOW_HANDOFF_HE.md
```

לא כלולים:

```text
node_modules/
.expo/
dist*/
.git/
קובצי QR זמניים
קובצי log
.DS_Store
```
