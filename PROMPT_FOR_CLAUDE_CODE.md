# פרומפט להדבקה בקלוד קוד על המחשב

העתיקי את כל מה שמתחת לקו והדביקי בקלוד קוד שרץ אצלך במחשב.

---

## המשימה

תבנה לי את אפליקציית BizFlow לאייפון דרך EAS Build, עד לקישור התקנה שאני
יכולה לפתוח מהטלפון. תעבוד עד הסוף ותדווח לי רק כשיש קישור או כשאתה חסום
במשהו שרק אני יכולה לפתור.

## הקוד

```bash
git clone https://github.com/valeriyakasp-cmyk/lera.git bizflow
cd bizflow
git checkout claude/friendly-newton-smp77j
```

שורש הפרויקט הוא שורש הריפו — `App.js`, `app.json` ו-`eas.json` נמצאים שם.

## מה זה הפרויקט — קרא לפני שאתה נוגע במשהו

זו אפליקציית Expo שעוטפת **WebView**. כל העיצוב והלוגיקה העסקית נמצאים
בקובץ אחד: `assets/bizflow/BizFlow.html`. זהו מקור האמת.

`App.js` מעתיק בזמן ריצה את `BizFlow.html`, את `support.txt` ואת שלוש
הספריות שב-`assets/bizflow/vendor/` אל תיקיית המסמכים של האפליקציה, ופותח
אותן ב-WebView.

**כללים שאסור להפר:**

- אל תשנה עיצוב, צבעים, אנימציות או טקסטים. שום דבר ויזואלי.
- תיקיית `src/` היא ניסיון ישן למימוש React Native. היא **לא** מחוברת
  לאפליקציה ולא מקור האמת. אל תבנה ממנה ואל תחבר אותה.
- אל תמחק את `assets/bizflow/vendor/`. שלושת הקבצים שם הם React, ReactDOM
  ו-Babel, והם ארוזים בכוונה כדי שהאפליקציה תעלה בלי אינטרנט. בלעדיהם
  המסך נתקע על מסך טעינה אינסופי.
- אל תריץ `expo install` או `expo-doctor` בלי צורך — הם משנים גרסאות.
  התלויות נעולות ועובדות.

## סביבה

- Node 22 ומעלה.
- pnpm 10. **שים לב:** מסמכים ישנים בפרויקט מפנים ל-pnpm 11.9.0 — גרסה
  כזו לא קיימת. השתמש ב-pnpm 10 העדכני.

```bash
pnpm install --frozen-lockfile
```

## אימות לפני בנייה

```bash
pnpm exec expo export --platform ios
```

חייב להסתיים בהצלחה עם **593 מודולים** ולארוז 5 נכסים:
`BizFlow.html`, `support.txt`, ו-`vendor/react.txt`, `vendor/react-dom.txt`,
`vendor/babel.txt`.

אם המספר שונה מהותית או שנכס חסר — עצור ותגיד לי. אל תמשיך לבנייה.

## הבנייה

```bash
pnpm exec eas-cli login
pnpm exec eas-cli build --profile preview --platform ios
```

הגדרות שכבר קיימות ואין לשנות:

```text
owner              lera2004
projectId          5dbf67d4-a3db-4bcc-8de2-3ef3aabff009
bundleIdentifier   com.valeriya.bizflow
פרופיל preview     distribution: internal
```

`internal` פירושו התקנה ישירה על מכשירים רשומים, בלי App Store ובלי
תהליך אישור.

## מה יקרה בבנייה הראשונה

EAS ישאל על חתימה. בחר **ניהול אוטומטי** — הוא ייצר את התעודה ואת פרופיל
ההקצאה מול חשבון ה-Apple Developer, וירשום את המכשיר שלי.

לשם כך צריך **חשבון Apple Developer בתשלום** (99$ לשנה). זו דרישה של אפל
לכל אפליקציה שמותקנת על אייפון. אם אין לי אחד — עצור ותגיד לי מיד, אל
תנסה לעקוף.

ייתכן שתתבקש להזין את ה-UDID של האייפון שלי. אם כן, תגיד לי איך להשיג אותו.

הבנייה רצה בשרתים של Expo, בערך 10–20 דקות. בסיום יש קישור התקנה.

## אחרי ההתקנה

שינוי ב-`assets/bizflow/BizFlow.html` לא מצריך בנייה מחדש:

```bash
pnpm run publish:preview
```

זה דוחף עדכון לערוץ `preview`, והאפליקציה המותקנת מושכת אותו בפתיחה הבאה.
בנייה מחדש נדרשת רק כששינו תלויות או הגדרות מקוריות.

## מתי לעצור ולשאול אותי

- אין חשבון Apple Developer בתשלום.
- אין גישה לחשבון Expo של `lera2004`.
- ה-export נכשל או מחזיר מספר מודולים שונה מהותית.
- EAS מבקש החלטה על תעודות שיכולה לבטל תעודה קיימת.

בכל מקרה אחר — תמשיך לבד עד שיש קישור התקנה.
